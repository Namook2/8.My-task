현재 메모리 상태로만 동작하는 할 일 데이터를 localStorage와 연동해줘.

요구사항:

1. localStorage 키는 "todos"로 하고, 할 일 배열을 JSON 문자열로 저장/조회
2. 할 일 추가, 수정, 삭제, 완료 체크 등 데이터가 바뀌는 모든 시점에
   자동으로 localStorage에 저장되도록 저장 함수를 만들고 각 액션 끝에서 호출
3. 페이지 로드 시 localStorage에 저장된 데이터가 있으면 불러와서 초기 렌더링,
   없으면 빈 배열로 시작
4. localStorage 데이터가 손상되었거나 파싱에 실패하는 경우를 대비해
   try/catch로 예외 처리하고, 실패 시 빈 배열로 안전하게 초기화

구현 후 새로고침해도 목록과 완료 상태, 필터 선택이 그대로 유지되는지
직접 시나리오를 설명하면서 확인해줘.
